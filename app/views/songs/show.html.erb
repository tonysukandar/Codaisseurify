<div class="row">
  <div class="col-md-9">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <h2>All the songs of <%= @artist.name %></h2>
      </div>
        <div class="panel-body">
        <% @artist.songs.each do |song| %>
        <div class="row">
          <div class="col-md-9">
            <h4><%= song.song_name %></h4>
          </div>
          <div class="col-md-3">
            <%= link_to "Delete", artist_song_path(@artist.id,song), class: "btn btn-primary", method: :delete, data: { confirm: "Are you sure?" } %>
          </div>
        </div>
        <% end %>
      </div>
    </div>
    <%= link_to "Add new song", new_artist_song_path(@artist), class: "btn btn-primary" %>
  </div>
</div>
